!(function () {
  if ("undefined" != typeof Prism) {
    ((Prism.languages["markup-templating"] = {}),
      Object.defineProperties(Prism.languages["markup-templating"], {
        buildPlaceholders: {
          value: function (r, e, n, a) {
            if (r.language === e) {
              var l = (r.tokenStack = []);
              ((r.code = r.code.replace(n, function (r) {
                for (var n = l.length; a.test(r); ) n = l.indexOf(r);
                return ((l[n] = r), "___" + e.toUpperCase() + n + "___");
              })),
                (r.grammar = Prism.languages.markup));
            }
          },
        },
        tokenizePlaceholders: {
          value: function (r, e) {
            if (r.language === e && r.grammar === Prism.languages.markup) {
              var n = new Prism.Token(e, r.code, r.grammar, r.code);
              ((r.grammar = Prism.languages[e]), (r.code = r.code));
              var a = r.tokenStack,
                l = function (r) {
                  for (var n = 0; n < r.length; n++) {
                    var i = r[n];
                    if ("string" != typeof i)
                      i.content && "string" != typeof i.content
                        ? l(i.content)
                        : i.content && l(i.tag.content);
                    else {
                      var o = e.toUpperCase(),
                        g = new RegExp("___" + o + "(\\d+)___", "g");
                      r[n] = i.replace(g, function (r, e) {
                        return new Prism.Token(e, a[e], "language-" + e, a[e]);
                      });
                    }
                  }
                };
              (l(n.content), (r.code = n.content));
            }
          },
        },
      }));
  }
})();
